var assert = require('assert');
var cssBackground = require('..');

describe('css-background', function() {

  it('should do something awesome', function() {
    assert.equal(cssBackground(), true);
  });
});
